[project]
name = "bridg"
version = "0.1.0"
description = "BRIDG persistence using SQLAlchemy"
dependencies = [
    "sqlalchemy<3.0.0,>=2.0.29",
    "cattrs<25.0.0,>=24.1.2",
    "toolz<2.0.0,>=1.0.0"
]

[dependency-groups]
test = [
    "pytest<9.0.0,>=8.3.3"
]

[build-system]
requires = ["uv_build>=0.8.12,<0.9.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-root = ""
