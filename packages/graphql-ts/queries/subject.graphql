query subject($id: UUID) {
  subject(id: $id) {
    id
    performingBiologicEntity {
      id
      type
      birthDate
      deathDate
      deathDateEstimatedIndicator
      deathIndicator
    }
  }
}
